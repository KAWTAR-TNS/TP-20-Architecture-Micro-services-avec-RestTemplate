server:
  port: 8081

spring:
  application:
    name: SERVICE-CLIENT  # Nom du service tel qu'il appara�tra dans Eureka
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/clientservicedb?createDatabaseIfNotExist=true
    username: "root"
    password: ""
  jpa:
    hibernate:
      ddl-auto: update  # Cr�e ou met � jour automatiquement le sch�ma de la base
      show-sql: true    # Affiche les requ�tes SQL g�n�r�es

# Configuration Eureka (� ajouter)
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  # URL du serveur Eureka
  instance:
    prefer-ip-address: true  # Utilise l'adresse IP plut�t que le nom d'h�te